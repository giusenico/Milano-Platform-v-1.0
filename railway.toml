# railway.toml — completo
[build]
builder = "NIXPACKS"
buildCommand = "npm ci"

[deploy]
startCommand = "node --max-old-space-size=512 server/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
